interface:
  display_name: "DOCX Create Edit"
  short_description: "Create new DOCX files and update existing ones."
  default_prompt: "Use $docx-create-edit to create a new DOCX from a prompt or update an existing one with a targeted, professional supporting statement."
