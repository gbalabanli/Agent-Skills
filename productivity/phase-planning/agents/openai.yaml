interface:
  display_name: "Phase Planning"
  short_description: "Create linked master plans and phase docs in docs/."
  default_prompt: "Analyze this project and produce docs/master_plan.md plus docs/phases/phase-XX-<slug>.md files, then keep docs/master_plan.md synchronized whenever a phase document changes."
