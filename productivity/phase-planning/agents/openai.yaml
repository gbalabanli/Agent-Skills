interface:
  display_name: "Phase Planning"
  short_description: "Create synchronized master and phase plans in docs/ (split or consolidated)."
  default_prompt: "Analyze this project and maintain a linked planning set in docs/: keep the master plan synchronized with phase artifacts, using either split phase files (docs/phases/phase-XX-<slug>.md) or an existing consolidated roadmap file (for example docs/PHASES.md)."
